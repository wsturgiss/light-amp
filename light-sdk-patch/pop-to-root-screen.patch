diff --git a/sdk/client/src/main/kotlin/com/thelightphone/sdk/LightScreen.kt b/sdk/client/src/main/kotlin/com/thelightphone/sdk/LightScreen.kt
index 020198d..44441b2 100644
--- a/sdk/client/src/main/kotlin/com/thelightphone/sdk/LightScreen.kt
+++ b/sdk/client/src/main/kotlin/com/thelightphone/sdk/LightScreen.kt
@@ -46,6 +46,17 @@ abstract class SimpleLightScreen<ResultType>(sealedActivity: SealedLightActivity
         this.result = result
         activity.goBack()
     }
+
+    /**
+     * SDK PATCH (additive, upstreamable): pop every screen above the initial one.
+     *
+     * A tool with a persistent bottom tab bar needs this: tapping a tab from a
+     * detail screen has to return to the tab host regardless of how deep the
+     * stack is, and `goBack()` only unwinds one level.
+     */
+    fun popToRoot() {
+        activity.popToRoot()
+    }
 }
 
 abstract class LightScreen<ResultType, VM : LightViewModel<ResultType>>(
